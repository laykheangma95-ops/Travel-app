export default function WebHome() {
  return (
    <main>
      <h1>Domner</h1>
      <p>
        Scaffold. Whether the existing storefront at the repository root moves
        here is still an open decision — see the root CLAUDE.md.
      </p>
    </main>
  );
}
