{
  "$comment": [
    "Advisories we have reviewed and accepted, with the reason and the exit plan.",
    "A package is only allowed to sit here if the ONLY available fix is a major",
    "version bump that needs planning on its own. Anything with a non-breaking fix",
    "must be fixed, not listed. scripts/audit-check.mjs fails CI on every high or",
    "critical advisory whose package is NOT listed below, so a newly introduced",
    "vulnerable dependency still breaks the build.",
    "Review this file whenever the Next.js upgrade lands."
  ],
  "packages": {
    "next": {
      "reason": "Every listed advisory is fixed only in next@16. This app is on Next 14 App Router and calls cookies() synchronously in app/layout.tsx, which Next 15+ forbids — the upgrade is a planned migration, not a dependency bump. The advisories that carry real weight here (Image Optimizer DoS, rewrite SSRF, cache poisoning) are self-hosting and rewrite/i18n issues; this app deploys on Vercel, defines no rewrites, and uses the App Router without i18n routing.",
      "acceptedUntilUpgrade": "next@16",
      "reviewed": "2026-08-02"
    },
    "postcss": {
      "reason": "Not a direct dependency — it is bundled under node_modules/next and only runs at build time on our own stylesheets, never on attacker-supplied CSS. Resolves when next is upgraded.",
      "acceptedUntilUpgrade": "next@16",
      "reviewed": "2026-08-02"
    },
    "sharp": {
      "reason": "Not a direct dependency — it arrives as an optionalDependency of next@15 in apps/web and apps/ops, which pins `sharp ^0.34.3`. The libvips CVEs are fixed in sharp@0.35, but 0.35 is outside that range: an npm override to 0.35 makes npm drop the optional dependency entirely, which silently disables next/image optimization in production. The first Next release that itself depends on sharp@>=0.35 is 16.3, so this is the same planned upgrade as the `next` and `postcss` entries above. Exposure today is nil — both apps are empty scaffolds that serve no images. Re-review before apps/web serves its first optimized image.",
      "acceptedUntilUpgrade": "next@16",
      "reviewed": "2026-08-12"
    },
    "glob": {
      "reason": "devDependency only, reached through eslint-config-next. The advisory is command injection through the glob CLI's -c/--cmd flag; we never invoke the glob CLI, and this package is not shipped to production.",
      "acceptedUntilUpgrade": "eslint-config-next@16",
      "reviewed": "2026-08-02"
    },
    "@next/eslint-plugin-next": {
      "reason": "devDependency only. Flagged solely because it depends on the glob version above.",
      "acceptedUntilUpgrade": "eslint-config-next@16",
      "reviewed": "2026-08-02"
    },
    "eslint-config-next": {
      "reason": "devDependency only. Flagged solely because it depends on @next/eslint-plugin-next above.",
      "acceptedUntilUpgrade": "eslint-config-next@16",
      "reviewed": "2026-08-02"
    }
  }
}
