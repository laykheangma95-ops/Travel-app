/**
 * SERVER ONLY — see the note in `crypto.ts`.
 *
 * Database-backed sliding-window rate limiter.
 *
 * PLACEHOLDER — Phase 2 relocates `_staging/src/lib/rate-limit.ts` here.
 * Guards the activation endpoint, which hands out bearer credentials.
 */

export {};
