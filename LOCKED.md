# 🔒 LOCKED — finished designs (do not change without asking)

These designs are **FINAL**. When adding new work, ADD it — do not remove,
revert, or restyle anything below unless the user asks for that exact change.

Live reference (the design we protect):
**https://travel-8dta13o2c-laykheangma95-ops-projects.vercel.app/**

Full details, the 1 → 10 registry, the rules, and the surgical per-section map
are in **[`docs/design-lock/`](./docs/design-lock/README.md)**.

Quick list:

1. Home — Hero (3D particle globe + title + CTAs) · `components/home/GlobeHero.tsx`
2. Home — Cambodia Showcase (spinning medallion ring) · `components/home/CambodiaShowcase.tsx`
3. Home — Feature cards · `components/home/HomeContent.tsx`
4. Home — How it works · `components/home/HomeContent.tsx`
5. Home — Popular destinations · `components/home/HomeContent.tsx`
6. Home — Testimonials · `components/home/HomeContent.tsx`
7. Home — Bottom CTA (night-sky band) · `components/home/HomeContent.tsx`
8. Brand tokens (colors/fonts) · `tailwind.config.ts` + `app/globals.css`

Save point: commit **`e239732`** (`git show e239732` to see the exact locked state).
This commit is the permanent recovery point — see
[`docs/design-lock/README.md`](./docs/design-lock/README.md#save-points-git-commit-shas).
